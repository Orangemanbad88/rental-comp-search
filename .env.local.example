# RentAtlas — Environment Configuration

# Data source: 'mock' (default) uses local JSON data, 'mls' uses live RETS
NEXT_PUBLIC_DATA_SOURCE=mock

# RETS / MLS Connection (required when DATA_SOURCE=mls)
RETS_LOGIN_URL=https://your-mls-server.com/rets/login
RETS_USERNAME=your_username
RETS_PASSWORD=your_password
RETS_USER_AGENT=RentAtlas/1.0

# NOTE: You may need to update the rental class in src/app/api/search/route.ts
# Common rental classes: RL_2, RE_2, RentalListing/RL_1
# Check your MLS metadata to confirm the correct resource/class for rentals.
